set(src_dirs
    inmp441_mic
    max98357a_amp
    st7789_lcd
    ws2812_led
)

set(include_dirs
    inmp441_mic
    max98357a_amp
    st7789_lcd
    ws2812_led
)

set(requires
    driver
    esp_driver_i2s
    esp_lcd
    led_strip 
    esp_timer
)

idf_component_register(
    SRC_DIRS      ${src_dirs}
    INCLUDE_DIRS ${include_dirs}
    REQUIRES     ${requires}
)

target_compile_options(${COMPONENT_LIB} PRIVATE
    -ffast-math
    -O3
    -Wno-error=format
    -Wno-format
)
